<h1 class="page-header text-center"> All lost bikes</h1>

{{#each model as |bike|}}
  {{#if bike.lost}}
    <ul>
      <li>{{#link-to 'bike' bike.id}}{{bike.make}}{{/link-to}}</li>
    </ul>
  {{else}}

  {{/if}}
{{/each}}
